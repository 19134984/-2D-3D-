# PTRT Ra=10^6 首个 P100 稳态测试

- 作业：`7044.master`，`PTRT_S6_N256`。
- 提交时间：2026-09-18 21:24:48（服务器时间）。
- 提交后一次核查：`job_state=R`，`exec_host=node07/48`，无依赖。
- 队列：batch；node07，1 CPU 进程，单 P100；walltime=24小时。
- node05 因 torque spool filesystem full 被标记 down；node07 提交前 P100 无运行进程，已有48核任务为其他用户CPU作业。
- 远端：`/data2/XLLi/PTRT/Ra1e6/sideheated_N256_20260918/`。
- 所有输出：上述路径下 `results/`；编译文件：`build/`；源码：`source/`。

## 算例

侧壁差温方腔，四壁无滑移，上下绝热；256×256 均匀网格。
Ra=1e6，Pr=0.71，Ma=0.1；P-TRT 流场、Luo/Wang D2Q5 温度场，从零启动。
沿用此前多块网格 Ra=1e6 的物理条件和细网格分辨率，但本算例不使用粗细块。

每2000步检查；errorU和errorT首次均不超过1e-7后停止；最大步数参数20000000。
保留基线循环 `itc<=itc_max`，极限退出实际可到20000001步，不能把达到上限当成收敛。
快照和Nu/Re每10 t_ff；检查点和Tecplot每100 t_ff；另保存真正停止时刻的f/g检查点。
宏观场有限性、正密度及收敛误差有限性每2000步检查。

仅测试副本增加诊断及最终检查点调用，数值碰撞、迁移、边界未改。
本地母版仍保持原设置，不受测试参数影响。

## 来源

提交时重新读取本地 `PTRT/2DRBOpenaccPTRT.F90`。文件名已由用户工作区改名，
内容哈希与前次验证版本相同。

- 本地母版：`390f8297ee60eec7e8b515e44d33570e77f0b583e512537d4a2562b97765d1cc`
- 参数化算例：`65c59be92219f44026a20889ff593601cbbd2c54a30e04a796e179ce349daa4b`
- PBS编译前实际源：`65c59be92219f44026a20889ff593601cbbd2c54a30e04a796e179ce349daa4b`

本地算例已通过 gfortran OpenACC 语法检查；上传后通过 bash -n，远端源哈希核对一致。
核查时PBS正在执行，已写入编译前哈希，尚未取得compile.status或run.status；
因此不宣称GPU编译成功、求解完成或达到稳态。

## 后续验收

检查 `compile.status`、`run.status`、`validation.status` 与完整日志；
检查收敛历史与实际最终检查点，独立重算Nu、Re和冷热壁热流。
`steady_threshold_passed_pending_checkpoint_audit` 仅表示文本阈值及错误扫描通过，
不代替二进制有限性检查或空间收敛验证。

本轮按提交任务结束，不持续轮询，也未提交Ra=1e7或Ra=1e8。
原始队列回执见 `submission_status.txt`，详细配置见 `manifest.json`。
