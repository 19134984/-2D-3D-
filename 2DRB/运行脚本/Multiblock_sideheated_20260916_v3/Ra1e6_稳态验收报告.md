# Ra=10⁶ 多块网格首个稳态算例验收

2026-09-16 核查。作业 6979.master，node05。结论：正常退出，通过源码规定的稳态阈值；与指定文献参考值接近，但尚不是网格无关性验证。

## 算例及完成状态

- 侧壁差温，四壁无滑移、上下绝热；Pr=0.71、Ma=0.1。
- 最细等效网格 256×256，粗细比 2；细层接口编号左/下 32，右/上 33。
- 原 D2Q9 MRT / 旧 D2Q5 MRT，无 EnableUseG 热流历史分支。
- 从零启动；834000 细步停止，实际终点 **188.0898923844 t_ff**。
- errorU=8.2112721801e-8，errorT=9.7740385200e-8，均不超过 1e-7；相邻检查相隔 2000 细步，首次同时达标后停止。
- 运行时间 2026-09-16 16:38:11 至 16:52:21（北京时间）；求解器计时 849.7048 秒，约 14 分 10 秒。共享 GPU，该耗时不作独占性能基准。
- compile.status=0，run.status=0，validation.status=steady_threshold_passed，外层任务记录退出码 0。
- 核查时 PBS 已不再保留该作业的 qstat 详情；完成判断来自正式结果文件与外层退出记录，不把查询不到作业单独当成成功证据。

## 最终指标与文献

**以下来自最终检查点的独立复算，时刻为 188.089892 t_ff。** 常规 Nu/Re 历史最后一次采样为 180.000223 t_ff；没有把它冒充最终时刻。

| 指标 | 本次最终值 | 文献最小网格 257² | 相对偏差 | 文献外推值 | 相对偏差 |
|---|---:|---:|---:|---:|---:|
| NuVolAvg | 8.83077421 | 8.8282 | +0.02916% | 8.8186 | +0.13805% |
| ReVolRMS | 99.26749024 | 99.6110 | -0.34485% | 99.3542 | -0.08727% |
| Nu_hot | 8.83485875 | 8.8509 | -0.18124% | 8.8206 | +0.16165% |

Nu_cold=8.83436778。冷热壁热流相对差为 0.005557%（分母为 Nu_hot）。

参考：仓库 `pdf/Interpolation-supplemented lattice Boltzmann simulation of thermal convection on non-uniform meshes.pdf`，第 6 页表 2，Ra=10⁶、Pr=0.71。该文使用非均匀 ISLBM；257² 与本次细等效 256²、中心粗块加细环的离散布局并不相同。相对偏差定义为 100×(本次−参考)/参考。

Re 按 sqrt(体积平均(u²+v²))×H/nu 复算。NuVolAvg 按 1+体积平均(uT)×H/alpha 复算。体积积分使用真实分区面积，外围细环扣除中心粗块区域，不重复计入重叠层或细环中心空区。

## 验收证据与边界

- 18 条 Nu/Re 历史、417 条收敛记录全部有限、严格时间递增；采样时刻与向上对齐到粗细同步步的计划一致，无缺段。
- 最终检查点完整读取至文件末尾；全部 f、g、u、v、T、rho、Fx、Fy、时间历史 p 与稳态前值 up/vp/Tp 都有限。
- 检查点网格、物理参数、累计步数、采样计数、末尾 errorU/errorT 与源码/文本一致。
- 运行日志未检出 CUDA_ERROR、Error、FATAL 或 NaN/Inf；编译源哈希与本地算例副本一致。
- 最终总质量相对初始均匀密度漂移 -0.00069544%，即约 -6.95 ppm。这里记录误差，不把它描述为严格质量守恒。
- 已验证稳态停止判据、完成状态、输出有限性及本分辨率下的参考值误差。
- 尚未验证不同网格/细层厚度/粗细比的收敛性，也未通过延长运行检查“连续多次满足阈值”。
- 本次未发现新的 GPU 并行执行错误；当前小幅文献偏差不能仅凭单网格结果归因为物理模型、接口离散或后处理问题。
- Ra=10⁷ 未提交。

## 路径与复算

远端：`/data2/XLLi/Multiblock/Ra1e6/ratio2/sideheated_N256_20260916_v3/`。

本地证据：本目录下 `Ra1e6/ratio2/sideheated_N256_20260916_v3/results/`；机器可读验收记录为同算例目录下 `验收结果.json`。

最终检查点：`reloadFile2DOpenaccMultiblock-000000834000.bin`。
SHA-256：`c93a5f93f19446694688ec6333bea639c673c8c4f11c41901640a7f9b1322dbf`。

算例源 SHA-256：`b87025159cf6e72efba7bed008417766a5a5b2d821895dae92cbb62d546b8732`。

本地复算命令：`python 运行脚本/tools/audit_multiblock_ra1e6.py`。不带 --fetch 时仅使用已下载证据，不连接服务器或改变远端计算。
