# Ra=10⁸ 多块网格稳态验收报告

2026-09-17 核查。作业 6981.master，node05。结论：正常完成，达到源码规定的稳态阈值；完整历史和最终检查点有限。Nu 相对文献外推值偏差约 0.30%–0.40%，仍需网格加密才能判断空间收敛。

## 算例及完成状态

- 稳态侧壁差温，四壁无滑移、上下绝热；Ra=1e8、Pr=0.71、Ma=0.1。
- 最细等效网格 512×512，粗细比 2，中心粗块加连通细环；细层编号左/下 64、右/上 65。
- D2Q9 MRT / 旧 D2Q5 MRT，无 EnableUseG；接口时间权重保留此前验证过的标量传递修复。
- 从零推进 **4580000 细步**，在 **516.4578579860 t_ff** 停止，未触及最大步数。
- errorU=9.6865653168e-8，errorT=1.7488425255e-8，均不超过 1e-7。每 2000 步检查，首次同时达标后停止；没有通过延长运行验证连续多次达标。
- 运行时间为北京时间 2026-09-16 22:03:30 至 2026-09-17 01:27:45；求解器计时 **12252.611 秒**，约 3 小时 24 分 13 秒。共享 GPU，不用作独占性能比较。
- compile.status=0、run.status=0、validation.status=steady_threshold_passed；PBS stdout/stderr 为空，求解器有正常结束计时。
- 核查时 qstat 已不保留该作业详情；成功判断依据完成记录、正常结束日志和最终检查点，不以“查询不到作业”作为成功证据。

## 最终结果

下表为最终检查点的独立复算，时刻 **516.457858 t_ff**。常规 Nu/Re 历史最后采样在 510.0001050142 t_ff，不将其冒充最终时刻。

| 指标 | 本次最终值 | 文献最小网格 513² | 相对偏差 | 文献外推值 | 相对偏差 |
|---|---:|---:|---:|---:|---:|
| NuVolAvg | 30.29559352 | 30.3130 | -0.05742% | 30.2041 | +0.30292% |
| ReVolRMS | 536.26297869 | 542.9352 | -1.22892% | 536.5456 | -0.05267% |
| Nu_hot | 30.33048563 | 30.3503 | -0.06529% | 30.2096 | +0.40016% |

Nu_cold=30.33066165。冷热壁热流相对差为 **0.00058034%**，分母为 Nu_hot。

参考：仓库 `pdf/Interpolation-supplemented lattice Boltzmann simulation of thermal convection on non-uniform meshes.pdf` 第 6 页表 2，Ra=10⁸、Pr=0.71。相对偏差=100×(本次−参考)/参考。论文的非均匀 ISLBM 513² 与本次最细等效 512² 的局部粗化布局不同，不是相同离散网格。

Re 按 sqrt(体积平均(u²+v²))×H/nu 复算，NuVolAvg 按 1+体积平均(uT)×H/alpha 复算；面积积分只计入各自分区，细环扣除中心粗块区域，不重复计入重叠区域。

## 验收证据及限制

- 51 条 Nu/Re 历史及 2290 条收敛记录均有限、严格递增，采样时刻与按粗细同步步对齐的计划一致，没有缺段。
- 最终 MB2DRESTART0010 检查点完整解析至文件末尾，全部 f、g、u、v、T、rho、Fx、Fy、接口历史 p、稳态前值 up/vp/Tp 均有限。
- 检查点网格、物理参数、累计步数、采样计数和末尾 errorU/errorT 与文本一致。
- solver.stdout 和 PBS stderr 未检出 NaN/Inf、CUDA_ERROR、FATAL 或 Error；编译前源码 SHA-256 与本地算例一致。
- 相对初始均匀密度的总质量漂移为 **-0.00110406%**，约 -11.04 ppm；记录为数值误差，不描述为严格守恒。
- 已验证正常完成、指定稳态判据、输出有限性及当前分辨率的文献误差。没有验证网格/粗细比/细层厚度收敛或长期稳定性。
- 与 Ra=10⁶、10⁷ 相比，本次 Nu 的文献外推偏差有所增大。不同 Ra 的三组成功运行不能代替同一 Ra 下的网格收敛研究；尚不能将偏差明确归因于物理模型、接口离散或后处理。

## 证据位置

远端：`/data2/XLLi/Multiblock/Ra1e8/ratio2/sideheated_N512_20260916_v3/`。

本地：本目录下 `Ra1e8/ratio2/sideheated_N512_20260916_v3/`，其中 `验收结果.json` 为数值摘要，`results/` 保存日志、历史和最终检查点。

- 检查点：`reloadFile2DOpenaccMultiblock-000004580000.bin`
- 检查点 SHA-256：`daade893e397a6eaf1571ca5b8840fc9fe920e18df7ebc6a9839e1ca813c49e3`
- 实际编译源码 SHA-256：`905a764abb8bec99eb218d678e97456328cb98192ff41b45420ac7e5c5a3bc30`
- 离线复算：`python 运行脚本/tools/audit_multiblock_ra1e8.py`。
